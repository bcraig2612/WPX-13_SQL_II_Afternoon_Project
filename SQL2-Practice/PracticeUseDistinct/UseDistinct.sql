------------------------------------ STEP 5 (USE DISTINCT) ------------------------------------
-- (1)
    SELECT DISTINCT composer
    FROM track;
-- (2)
    SELECT DISTINCT billing_postal_code
    FROM invoice;
-- (3)
    SELECT DISTINCT company
    FROM customer;
